<Page
    x:Class="WebcatApp.View.PdfPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WebcatApp.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"    
    DataContext="{Binding Source={StaticResource Locator}, Path=PdfPage}"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls" 
    xmlns:viewmodel="using:WebcatApp.ViewModel" 
    d:DataContext="{d:DesignInstance Type=viewmodel:PdfPageModel}"
    mc:Ignorable="d" x:Name="root">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" x:Name="Grid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel>
            <TextBlock Text="Download PDF"
                           Style="{StaticResource TitleTextBlockStyle}" 
                           Margin="8,0,0,24"/>
            <Button Content="Download PDF" Style="{StaticResource AccentButtonStyle}" 
                    Command="{Binding DownloadPDFFile}" 
                    Margin="25,200,0,0" Height="30" 
                    Width="200"/>
        </StackPanel>
        <ScrollViewer ZoomMode="Enabled" Background="DarkGray" Grid.Column="1">
            <ItemsControl ItemsSource="{Binding PdfPages}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Image Source="{Binding}" Margin="0 2" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>

    <!--<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" x:Name="Grid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <TextBlock Text="Download PDF"
                           Style="{StaticResource TitleTextBlockStyle}" 
                           Margin="8,0,0,24"/>
            <TextBlock Text="La segunda pantalla que llamaras PDF, debes obtener el archivo (https://gotocon.com/dl/goto-aar-2014/slides/JamesMontemagno_XamarinFormsNativeIOSAndroidAndWindowsPhoneAppsFromONECCodebase.pdf) usando el metodo de peticiones REST que prefieras y luego guardarlo de manera local por codigo. Cuando ya este guardado como StorageFile deberas mostrarlo en la pantalla con el control que prefieras dentro del app, no se permite mostrarlo usando un Launcher ni otra app."
                    Style="{StaticResource BodyTextBlockStyle}" Width="270"
               />
            <Button Content="Download PDF" Command="{Binding DownloadPDFFile}" Margin="25,10,0,0" Height="30" Width="250"/>
        </StackPanel>
        <ScrollViewer ZoomMode="Enabled" Background="DarkGray" Grid.Column="1">
            <ItemsControl ItemsSource="{Binding PdfPages}" Height="670" Width="1200">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Image Source="{Binding}" Margin="1 3" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>-->
</Page>
